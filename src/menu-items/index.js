import preparedMenuItems from './prepare-menu-items';

// ===========================|| MENU ITEMS ||=========================== //

const menuItems = {
    items: preparedMenuItems
};

export default menuItems;
